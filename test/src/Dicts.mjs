// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Spice from "./Spice.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function stringDict_encode(v) {
  return Spice.dictToJson(Spice.stringToJson, v);
}

function stringDict_decode(v) {
  return Spice.dictFromJson(Spice.stringFromJson, v);
}

function intDict_encode(v) {
  return Spice.dictToJson(Spice.intToJson, v);
}

function intDict_decode(v) {
  return Spice.dictFromJson(Spice.intFromJson, v);
}

function inner_encode(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "id",
      Spice.intToJson(v.id)
    ],
    [
      "name",
      Spice.stringToJson(v.name)
    ]
  ]));
}

function inner_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let id = Stdlib_Option.getOr(Stdlib_Option.map(v["id"], Spice.intFromJson), Spice.error(undefined, "id" + " missing", v));
  if (id.TAG === "Ok") {
    let name = Stdlib_Option.getOr(Stdlib_Option.map(v["name"], Spice.stringFromJson), Spice.error(undefined, "name" + " missing", v));
    if (name.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          id: id._0,
          name: name._0
        }
      };
    }
    let e = name._0;
    return Spice.error("name", e.message, e.value);
  }
  let e$1 = id._0;
  return Spice.error("id", e$1.message, e$1.value);
}

function innerDict_encode(v) {
  return Spice.dictToJson(inner_encode, v);
}

function innerDict_decode(v) {
  return Spice.dictFromJson(inner_decode, v);
}

function dictT_encode(v) {
  return Spice.dictToJson(Spice.stringToJson, v);
}

function dictT_decode(v) {
  return Spice.dictFromJson(Spice.stringFromJson, v);
}

export {
  stringDict_encode,
  stringDict_decode,
  intDict_encode,
  intDict_decode,
  inner_encode,
  inner_decode,
  innerDict_encode,
  innerDict_decode,
  dictT_encode,
  dictT_decode,
}
/* No side effect */
